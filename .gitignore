node_modules/
dist/
coverage/
.wrangler/
*.log

# Secrets / local env — NEVER commit. Only the .example file is tracked.
.dev.vars
.env
.env.*
!.dev.vars.example

.DS_Store

# Local CLI tooling (mcp-publisher binary)
.tools/
